# 技能名称

## 概述

一句话说明这个技能做什么。这里不写"何时使用"——触发条件属于 description。

## 工作流程

1. 步骤一——具体动作，用祈使句。
2. 步骤二——如果满足条件 X，执行 A；否则跳到步骤 4。
3. 步骤三——……
4. 步骤四——……
   （注：所有写作规范如语态、强度标记、输出格式等，应直接内嵌在此处的步骤指令中）

## 示例

2–3 个最小输入/输出对。代码优于散文。

## 边界情况 / 坑

- 列出违反直觉、模型无法自行推断的环境事实，每条写成"现象 → 原因 → 处置动作"。

## 验证

1. 运行 `python scripts/validate.py --output <文件>`（换成你自己的验证脚本或清单）。
2. 如果 exit code ≠ 0，读取 stderr，修复后重跑。
3. 重复直到 exit code == 0。禁止跳过本步骤。

## References

- `references/foo.md` — 何时读取：[具体触发条件]
- `scripts/bar.py` — 何时运行：[具体触发条件]