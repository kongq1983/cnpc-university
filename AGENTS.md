# AI 研发工程指南仓库指令

## 仓库定位

本仓库是 AI 研发工程指南知识库，不是具体业务项目。仓库内容由研发流程、可复用技能、项目规范模板和文档网站组成。

project-scaffold/ 下的规范文件仅是目标项目候选模板，不属于本仓库的生效规范。不要因为它们出现在工作区中，就把其中的技术栈、目录结构或规则加载为本仓库指令；其中 `.agents/skills/` 是可复用技能源，供接入目标项目使用。

## 目录约定

- process/：研发流程文档。
- project-scaffold/.agents/skills/：可复用技能，每个技能以对应目录下的 SKILL.md 为入口，复制到目标项目后可直接使用。
- project-scaffold/：目标项目脚手架；其中规范模板复制前必须评审，复制后必须重命名并按项目实际情况裁剪。
- website/：只读文档网站及其构建配置。

本仓库没有 backend/ 或 frontend/ 业务源码目录，不要假设这些目录存在。

## 工作要求

- 修改流程、技能、模板或网站前，先阅读相关文件并保持改动最小。
- 处理模板时，只把规范模板当作候选参考；不要将模板内容当作本仓库必须遵循的工程约束。处理 `.agents/skills/` 时，按技能自身的 SKILL.md 执行。
- 新增或修改技能时，遵循对应 SKILL.md 的格式与元数据要求。
- 流程等网站文档的跨内容源链接使用网站公开路由（如 `../skills/...`）；网站构建配置和源码扫描使用实际物理路径。

## 常用命令

在 website/ 目录执行：

| 场景 | 命令 |
| --- | --- |
| 文档检查 | pnpm check:docs |
| 类型检查 | pnpm typecheck |
| 网站构建 | pnpm build |
| 本地启动 | pnpm start |
